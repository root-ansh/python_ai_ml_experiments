import turtle
turtle.speed(10)
for k in range(8):
    
    for i in range(8):
        turtle.forward(50)
        turtle.right(45)
    turtle.left(45)
    turtle.forward(50)
    for j in range(8):
        turtle.forward(50)
        turtle.right(45)
